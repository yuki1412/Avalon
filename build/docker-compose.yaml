services:
  web:
    build:
      context: ..
      dockerfile: build/Dockerfile
    ports:
      - 8080:8080
    volumes:
      - ../:/app
    environment:
      - GO_ENV=development 
    tty: true
